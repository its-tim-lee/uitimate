Instead of combining button + badge, we create separate components for each.

Scalable and Flexible:
- Iconify: We don't recommend other icon libs, cuz it may not
work as expected
- Tailwind CSS: We use Tailwind CSS for styling, cuz it's more flexible and
scalable than other CSS frameworks.

