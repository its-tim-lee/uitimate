---
import { startCase } from "lodash-es";
import "@/index.css"; // according to https://ui.shadcn.com/docs/installation/astro
import BaseLayout from "@/layouts/BaseLayout.astro";
import DocPageLayout from "@/layouts/DocPageLayout.astro";
import ComponentPageUsage from "@/components/internal/ComponentPageUsage.tsx";
import ComponentPageHero from "@/components/internal/ComponentPageHero.tsx";
import { Heading } from "@/components/ui/Heading/Heading";
const routeName = Astro.url.pathname.split("/").filter(Boolean).pop();
const anatomy = `<Skeleton/>`;
---

<BaseLayout>
  <DocPageLayout>
    <ComponentPageHero
      client:only="react"
      title={startCase(routeName)}
      subtitle="Use to show a placeholder while content is loading."
    />

    <br />

    <Heading
      size="h2"
      title="Usage"
      subtitle="How you can play this component."
    />
    <ComponentPageUsage
      demoId="skeleton-demo"
      anatomy={anatomy}
      client:only="react"
      preview="This component has no any special prop you need to work on, but the example code demo below is dead-simple to let you use it immediatley and effortlessly."
    />
  </DocPageLayout>
</BaseLayout>
