---
import { startCase } from "lodash-es";
import "@/index.css"; // according to https://ui.shadcn.com/docs/installation/astro
import BaseLayout from "@/layouts/BaseLayout.astro";
import DocPageLayout from "@/layouts/DocPageLayout.astro";
import ComponentPageHero from "@/components/internal/ComponentPageHero.tsx";
import { Heading } from "@/components/ui/Heading/Heading";
import { Badge } from "~/src/components/ui/Badge/Badge";
const routeName = Astro.url.pathname.split("/").filter(Boolean).pop();
const apiLink = `https://github.com/Aljullu/react-lazy-load-image-component#readme`;
---

<BaseLayout>
  <DocPageLayout>
    <ComponentPageHero
      apiLink={apiLink}
      title={startCase(routeName)}
      subtitle="lazy load images."
    />

    <br />
    <br />

    <Heading
      size="h2"
      title="Design Notes"
      subtitle="The implementation details that some would be appreciated."
    />

    <p>
      Currently, we use this component internally in our <Badge
        variant="secondary">Avatar</Badge
      > component, so may you check the source code and example there, or refer to
      the corresponding doc.
    </p>
  </DocPageLayout>
</BaseLayout>
