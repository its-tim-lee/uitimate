{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@changesets/cli": "2.28.1"
  },
  "scripts": {
    "cs": "changeset && cursor $(ls -t .changeset/*.md | head -n 1)",
    "ver": "changeset version",
    "pub": "changeset publish"
  }
}