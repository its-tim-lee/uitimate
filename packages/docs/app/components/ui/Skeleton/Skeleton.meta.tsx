import type { ComponentMeta } from "#/types/index.ts";

const meta: ComponentMeta = {
  description: 'A placeholder loading state that mimics the shape and structure of content while it is being loaded.',
}

export default meta