import type { ComponentMeta } from "#/types/index.ts";

export default {
  description: "The wrapper (just forwarding) of Radix's Slot.",
  anatomy: ``,
  tags: {
    root: ['SEALED']
  }
} as ComponentMeta;