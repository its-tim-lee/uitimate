import type { ComponentMeta } from "#/types/index.ts";

export default {
  description: "The wrapper (just forwarding) of Radix's AccessibleIcon.",
  anatomy: ``,
  tags: {
    root: ['SEALED']
  }
} as ComponentMeta;