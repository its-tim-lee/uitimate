import type { ComponentMeta } from "#/types/index.ts";

const meta: ComponentMeta = {
  description: "Make groups/layouts resizable with draggable handles.",
  anatomy: `
<ResizablePanelGroup>
  <ResizablePanel/>
  <ResizableHandle />
  <ResizablePanel/>
</ResizablePanelGroup>`
}

export default meta