export default function ConcertsMine() {
  return <div>
    You can visit this page via "/concerts/mine",
    but it doesn't adopt "layout nesting" behavior,
    so its layout will only be = app/root.tsx
  </div>;
}
